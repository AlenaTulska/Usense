import { PrioritizeDirective } from './prioritize.directive';

describe('PrioritizeDirective', () => {
  it('should create an instance', () => {
    const directive = new PrioritizeDirective();
    expect(directive).toBeTruthy();
  });
});
